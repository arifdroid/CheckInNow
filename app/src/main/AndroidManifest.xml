<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.checkinnow">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".registration_pack.Registration_Activity"
            android:label="@string/title_activity_main__menu_"></activity>
        <activity android:name=".registration_pack.RegistrationTestActitvity" />
        <activity android:name=".registration_pack.Registration_Activity_V2" />
        <activity android:name=".LogInActivity" />
        <activity android:name=".main_menu_fragment.Main_Menu_Activity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>